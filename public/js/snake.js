/* =========================================================
   snake.js — placeholder for future Snake game
   For now just decorative.
   ========================================================= */

(function () {
  const mount = document.getElementById('snake-mount');
  if (!mount) return;
  // No-op for V1. Real snake game comes later.
})();
